class TopNav {
  constructor(products, services, company, people, contact) {
    (this.products = products),
      (this.services = services),
      (this.company = company),
      (this.people = people),
      (this.contact = contact);
  }
}
export default TopNav;
